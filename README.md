# 开饭了助手 · Windows

一个可以置顶在其他应用前面的长条小工具，自动读取剪贴板内容并发送到手机"开饭了" App。

## 功能

- 🖥️ **窗口置顶** —— 永远在最前面，不挡视线
- 📋 **剪贴板监听** —— 复制抖音分享文本自动解析
- 🔍 **自动发现手机** —— 扫描局域网 8848 端口 + UDP 广播，不用手动填 IP
- 📤 **一键发送** —— 点发送按钮，手机输入框自动填入
- 🎲 **生成名字** —— 一键生成贴近现实的中文姓名并复制到剪贴板
- 🖥️ **高分辨率自适应** —— 按屏幕分辨率自动缩放，可在托盘菜单手动调倍数
- 🎨 **深色 / 浅色 UI** —— 跟随系统，可在托盘菜单切换
- 📌 **托盘图标** —— 关闭到托盘，双击恢复

## 使用

### 手机端准备

1. 打开"开饭了" App
2. 设置 → 局域网服务 → **启用局域网输入**
3. 保持 App 在前台

### Windows 端

1. 双击 `KaiFanLe-Helper.exe` 运行（单文件，无需安装）
2. 窗口出现在屏幕右上角
3. 复制抖音分享文本（比如在微信里）
4. 工具自动解析剧名并显示
5. 点「发送」按钮 → 手机输入框自动填入
6. 手机上点「新增」完成统计

### 拖动

鼠标按住窗口任意位置可拖动到屏幕任何角落。

### 关闭

点「✕」按钮 → 缩到托盘（右下角）
双击托盘图标 → 恢复窗口
右键托盘 → 更多功能 / 退出程序

### 右键托盘菜单

| 菜单项 | 说明 |
|--------|------|
| 显示窗口 | 恢复主窗口 |
| 重新扫描 | 立即重新发现手机 |
| 选择设备 | 多台设备时切换 |
| 历史记录 | 查看已解析的剧名历史 |
| 生成名字并复制 | 生成随机中文姓名复制到剪贴板 |
| 缩放 | 调整界面大小：自动 / 1.25x / 1.5x / 1.75x / 2.0x |
| 主题 | 自动（跟随系统）/ 浅色 / 深色 |
| 退出 | 关闭程序 |

> 💡 **生成名字**：主窗口上的 🎲 按钮和托盘菜单均可触发。姓名为单姓 + 名字（85% 双字名 / 15% 单字名），名字取自近年热门用字，贴近真实取名习惯。

> 💡 **缩放**：默认「自动」会按屏幕分辨率自适应。若仍觉得偏小，可在托盘菜单选更大倍数；设置会保存，重启后仍生效。

## 构建

已配置 GitHub Actions，push 到 `main` 后自动构建，产物为单文件 `dist/KaiFanLe-Helper.exe`（附 `.sha256` 校验）。

- 构建环境：Windows + Python 3.8.10 + PySide2 5.15.2.1 + PyInstaller 5.13.2
- 打包模式：`--onefile`（单文件）

## 注意事项

- 手机和电脑必须在**同一 WiFi**
- Windows 防火墙可能拦截局域网扫描，首次运行需允许（项目提供 `init_firewall.bat` 一键配置）
- 只扫描 8848 端口，手机端必须用这个端口
- 若使用了系统代理（如 Clash），局域网请求已自动绕过代理，不会受代理延迟影响
- 若 exe 被 Windows Defender 误报/拦截（PyInstaller 打包程序常见），可将 exe 所在文件夹加入 Defender 排除项

## 常见问题

**Q: 运行时报 `Cannot open PyInstaller archive`？**
A: 这是 exe 被 Windows Defender 拦截或下载损坏导致的。先对比 `.sha256` 校验文件确认完整，再将 exe 所在文件夹加入 Defender 排除项。

**Q: 发送后手机要很久才收到？**
A: 若开启了系统代理（Clash/VPN 等），旧版本会走代理导致 8-10 秒延迟。新版已改用直连（`http.client`）绕过代理，正常应秒达。

**Q: 界面在高分辨率屏幕上太小 / 太大？**
A: 程序会按屏幕分辨率自动缩放；如需微调，右键托盘图标 → 缩放 → 选择倍数。
